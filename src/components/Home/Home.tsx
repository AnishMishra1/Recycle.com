// Dependencies
import { useDispatch } from "react-redux";

// Local Files


const Home = () => {
  

  window.scrollTo({
    top: 0,
    behavior: "smooth",
  });

  return (
    <div className="max-w-[100vw] overflow-x-hidden">
      hello
    </div>
  );
};

export default Home;
